{% extends "base.html.twig" %}
{% block title 'Tous nos biens'%}

{% block body %}

<div class="jumbotron">
    <div class="container">
        {{ form_start(form) }}
        <div class="form-row align-items-end">
            <div class="col">
                {{ form_row(form.minSurface) }}
            </div>
            <div class="col">
                {{ form_row(form.maxPrice) }}
            </div>
            <div class="col">
                {{ form_row(form.options) }}
            </div>
            <div class="col">
                <div class="form-group">
                    <button class="btn btn-primary">Rechercher</button>
                </div>
            </div>
        </div>
        {{ form_end(form) }}
    </div>
</div>
<div class="container mt-4">
    <h1>Voir tous nos biens</h1>
    <div class="mr-5 mb-4">
        <a class="btn btn-primary" href="{{ path('admin.property.index') }}">Gérer les biens</a>
        <a class="btn btn-primary" href="{{ path('admin.option.index') }}">Gérer les options</a>
    </div>
    <!--div>
        '{ form_start(form) }'

        <div class="row">
            <div class="col-md-2"> </div>
            <div class="col-md-8 text-center mb-2">
                ' {form_row(form.text)} '
                <button class="btn btn-primary">Rechercher</button>

            </div>
            <div class="col-md-2"> </div>
        </div>
        { form_end(form) }

    </div-->
    <div class="row">
        {% for property in properties %}

                <div class="col-md-4">
                    {% include 'property/_property.html.twig' %}
                </div>

        {% endfor %}
    </div>
    <div class="navigation">
        {{ knp_pagination_render(properties) }}
    </div>
</div>

<div class="container">
</div>
{% endblock %}{# empty Twig template #}
